import QtQuick 2.15

Rectangle {
    width: 300
    height: 400
    color: "#444" // Gris pour simuler la zone grisée

    Text {
        anchors.centerIn: parent
        text: "Zone de transcription"
        color: "#fff"
    }
}
